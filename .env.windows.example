# 与 docker-compose.yml + docker-compose.windows.yml 配套：Exporter 在宿主机，Prometheus 抓取 host.docker.internal
PROMETHEUS_CONFIG_FILE=./deploy/prometheus/prometheus.windows.yml

# 仅与「容器内跑 exporter」有关；宿主机跑 exporter 时可忽略，但勿与下面 host 模式混用同一进程
# CLASH_HOST=host.docker.internal:9090
